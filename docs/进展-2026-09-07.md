# 进展记录 2026-09-07

> semantica-graph-server 当日工作记录；提交均已推送 `origin`（GitHub）与 `tiancloud`（内部镜像），工作区干净。

## 当日提交
- `916b2a9` Celery 接入真实 Redis（compose 内置 redis + 投递链路）+ MCP/CLI 镜像冒烟与镜像精简评估方案
- `1cd7fac` 委派执行契约限定 qoderclicn 任务范围（coding/文档/单测；复杂构建与系统级操作不委派）
- `5042863` 语义检索先行骨架（qoderclicn 委派）：守卫式 adapter + service/MCP 接线 + 单测（20 passed）
- `23d4486` 委派契约扩为 qoderclicn / jscode 双执行方
- `02401b1` HTTP async 建图提交前校验图谱存在（缺失 404）+ async 链路单测（25 passed）
- `ec25044` Celery 复用宿主本地 Redis（不启动 redis 容器）；compose/entrypoint/冒烟/文档/看板同步 P1 完成
- `38d4075` **OxigraphStore SPARQL/RDF 视图 + LLM 建图增强**（守卫式适配层/五面接口/单测 36 passed；方案与解决方案文档同步）
- `e9d6635` docs: 新增整体处理流程图（`docs/整体处理流程图.svg`）

## 本会话决定
- semantica-graph-server **不发布到 PyPI**：包体积偏大、定位为对外服务编排仓库；PyUploadX 已单独维护 PyPI 发布流程。源码发布走 git 双远端（origin/tiancloud）。
- RDF/SPARQL 视图以 SQLite 活动记录实时映射 OxigraphStore（只读补强，无缓存漂移）；LLM 建图增强默认关闭（env/provider 门控），两者均守卫式降级，不阻塞核心能力。
- 全量回归 `36 passed`；注意：starlette `TestClient` 在本沙箱 seccomp 下会卡死（最小复现已确认），验证需在非沙箱环境执行。

## 遗留/后续
- LLM 增强真实 provider 出网验收（需 API key）；镜像内 `pyoxigraph` 依赖确认（Dockerfile/requirements 增补）。
- SPARQL 白名单/超时/分页护栏；持久化 RDF 视图（增量索引）暂不做。
- 看板 P2 未完成：semantica 原生 MCP/检索接入（pinecone 依赖修复后）、镜像体积精简落地、MCP/CLI 镜像内冒烟脚本。

## 远端状态
- `origin/main` = `tiancloud/main` = `e9d6635`；工作区干净。
