# 引擎运行时依赖（与 pyproject.toml [project].dependencies 保持同步）。
# 构建镜像时单独安装该层：依赖只随本文件变更而重新下载，
# 代码改动不会重复拉取 torch 等超大依赖（见 Dockerfile builder 阶段）。
# RDF/SPARQL 视图依赖 pyoxigraph（semantica tripletstore-oxigraph extra，
# 当前 0.5.9 已在开发 venv 验证），镜像构建安装此 extra 后两能力可用。
semantica[tripletstore-oxigraph]==0.6.5
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.0
grpcio>=1.81
protobuf>=5.29
# celery[redis]：worker 的 broker/backend 是 Redis，必须带 redis extra（redis-py），
# 否则容器内 kombu 的 redis 传输导入失败（'NoneType' has no attribute 'Redis'），
# worker 一启动即崩、异步作业永久 pending。开发 venv 因已装 redis 8.1.0 掩盖了该缺口。
celery[redis]>=5.3
typer>=0.12
# 跨层共享模型/协议 SDK（跨仓归位契约）：图谱资产对象（G8）、traceId 纯函数（G2）、
# 引擎作业视图与状态映射（G3）。引擎 domain/application/interfaces 直接 import ikc_sdk，
# 镜像构建必须安装（PyPI 已有 0.8.3，2026-09-16 起）。
ikc-sdk-lib==0.8.3
